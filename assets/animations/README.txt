Desktop pet animation assets
===========================

Folders:
  idle/      4 frames
  wave/      4 frames
  sleep/     4 frames
  thinking/  3 frames

All frames are exported as transparent PNG files on a 512x512 canvas and bottom-aligned for easier animation playback.
The thinking sheet source only contained 3 practical frames, so thinking was exported as 3 frames.
See manifest.json for frame counts and recommended FPS.
